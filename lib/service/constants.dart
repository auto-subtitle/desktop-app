import 'package:flutter/material.dart';
import 'package:material_color_generator/material_color_generator.dart';

MaterialColor primaryColor =
    generateMaterialColor(color: const Color(0xff647C90));

MaterialColor secondaryColor =
    generateMaterialColor(color: const Color(0xffe2ded0));

MaterialColor accentColor =
    generateMaterialColor(color: const Color(0xff4e4f50));

MaterialColor subAccentColor =
    generateMaterialColor(color: const Color(0xff7a7a7a));
